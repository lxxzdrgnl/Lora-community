# 개발 중 이슈 및 해결 방법

## 1. N+1 문제 해결 (GeneratedImage - GenerationHistory)

### 문제 상황
- `GeneratedImage` 엔티티가 `GenerationHistory`와 ManyToOne 관계
- `GeneratedImageResponse`에서 prompt, steps 등의 정보를 가져올 때 `image.getGenerationHistory().getPrompt()` 형태로 접근
- 여러 이미지를 조회할 때 각 이미지마다 GenerationHistory를 추가로 조회하는 N+1 문제 발생 가능

### 해결 방법
Repository에서 **JOIN FETCH**를 사용하여 한 번의 쿼리로 GeneratedImage와 GenerationHistory를 함께 조회

```java
@Query("SELECT gi FROM GeneratedImage gi JOIN FETCH gi.generationHistory gh " +
        "WHERE gh.model.id = :modelId AND gi.isSample = true " +
        "ORDER BY gi.displayOrder ASC")
List<GeneratedImage> findSampleImagesByModelId(@Param("modelId") Long modelId);
```

### 고려했던 대안
- GeneratedImage에 prompt, steps 등의 필드를 직접 추가 (비정규화)
- 장점: JOIN 불필요, 조회 빠름
- 단점: 데이터 중복, 관리 복잡

### 최종 선택
- **정규화된 구조 유지 + JOIN FETCH 사용**
- 이유: 데이터 일관성 유지, JOIN FETCH로 성능 문제 해결
- 위치: `GeneratedImageRepository.java`

---

## 2. GenerationHistory 삭제 시 Cascade 동작

### 동작 방식
`GenerationHistory` 엔티티에 다음과 같이 설정되어 있음:

```java
@OneToMany(mappedBy = "generationHistory", cascade = CascadeType.ALL, orphanRemoval = true)
private List<GeneratedImage> generatedImages = new ArrayList<>();
```

- **cascade = CascadeType.ALL**: GenerationHistory의 모든 영속성 작업이 GeneratedImage에 전파
- **orphanRemoval = true**: 관계가 끊어진 GeneratedImage 자동 삭제

**결과**: GenerationHistory를 삭제하면 연관된 모든 GeneratedImage도 함께 삭제됨

### 문제점
샘플 이미지로 사용 중인 GeneratedImage가 있는 경우, GenerationHistory를 삭제하면 모델의 샘플 이미지가 의도치 않게 삭제될 수 있음

### 해결 방법
GenerationHistory 삭제 전에 샘플 이미지 존재 여부를 확인하고, 있으면 삭제를 막음

```java
@Transactional
public void deleteGenerationHistory(Long historyId, Long userId) {
    // ... 권한 확인 ...

    // 샘플로 사용 중인 이미지가 있는지 확인
    boolean hasSampleImages = history.getGeneratedImages().stream()
            .anyMatch(GeneratedImage::getIsSample);

    if (hasSampleImages) {
        throw new CustomException(ErrorCode.INVALID_INPUT_VALUE,
                "샘플로 사용 중인 이미지가 있어 삭제할 수 없습니다. 먼저 샘플 선택을 해제해주세요.");
    }

    generationHistoryRepository.delete(history);
}
```

### 사용자 플로우
1. 사용자가 샘플로 사용 중인 이미지를 포함한 GenerationHistory 삭제 시도
2. 에러 메시지와 함께 삭제 실패
3. 사용자가 먼저 샘플 선택 해제 (`unmarkImageAsSample`)
4. GenerationHistory 삭제 성공

**위치**: `GenerationService.deleteGenerationHistory()`

---

## 3. ModelSample 테이블 분리 리팩토링 (희소 데이터 문제 해결)

### 문제 상황
초기 설계에서는 `GeneratedImage` 테이블에 `isSample` 필드와 `displayOrder` 필드를 추가하여 샘플 이미지를 관리하려 했음.

```java
// 문제가 있던 초기 설계
@Entity
public class GeneratedImage {
    private Boolean isSample;  // 샘플 여부
    private Integer displayOrder;  // 샘플 순서
}
```

### 발견된 문제점

1. **희소 데이터(Sparse Data) 문제**
   - 전체 생성 이미지 중 극소수만 샘플로 선택됨
   - 예: 1,000,000개 이미지 중 10개만 샘플 → 999,990개는 `isSample=false`, `displayOrder=NULL`
   - 대부분의 행에서 불필요한 컬럼이 NULL 값으로 낭비됨

2. **관심사의 혼합**
   - `GeneratedImage`는 **로그/히스토리 데이터** (모든 생성 기록)
   - 샘플은 **큐레이션 데이터** (선별된 대표 이미지)
   - 두 가지 다른 목적이 한 테이블에 혼재

3. **쿼리 성능 문제**
   - 샘플 이미지 조회 시 수백만 행의 `generated_images` 테이블 스캔 필요
   - 인덱스를 추가해도 대부분 NULL 값이라 효율 낮음

### 해결 방법: 별도 테이블 분리

**ModelSample 테이블 생성**
```java
@Entity
@Table(name = "model_samples")
public class ModelSample extends BaseEntity {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "model_id", nullable = false)
    private LoraModel model;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "generated_image_id", nullable = false)
    private GeneratedImage generatedImage;  // FK 참조

    @Column(name = "display_order", nullable = false)
    private Integer displayOrder;

    @Column(name = "is_primary", nullable = false)
    private Boolean isPrimary;
}
```

### 개선 효과

1. **데이터 밀도 향상**
   - `model_samples` 테이블: 모든 행이 실제 샘플 데이터
   - 희소 데이터 없음, 공간 효율성 향상

2. **쿼리 성능 대폭 개선**
   - 샘플 조회 시 작은 `model_samples` 테이블만 스캔 (수십~수백 행)
   - `generated_images` 테이블 스캔 불필요 (수백만 행 회피)

3. **관심사 분리**
   - `generated_images`: 순수 로그/히스토리 (변경 없음)
   - `model_samples`: 큐레이션 (추가/삭제/순서변경)

4. **확장성**
   - 샘플 관련 필드 추가 시 `model_samples`에만 영향
   - `generated_images` 테이블은 안정적으로 유지

### N+1 문제 해결
ModelSample 조회 시 JOIN FETCH로 GeneratedImage와 GenerationHistory를 함께 로드:

```java
@Query("SELECT ms FROM ModelSample ms " +
        "JOIN FETCH ms.generatedImage gi " +
        "JOIN FETCH gi.generationHistory gh " +
        "WHERE ms.model.id = :modelId " +
        "ORDER BY ms.displayOrder ASC")
List<ModelSample> findByModelIdOrderByDisplayOrder(@Param("modelId") Long modelId);
```

### 고려했던 대안

1. **isSample 필드 유지 + 인덱스 추가**
   - 장점: 구조 변경 최소화
   - 단점: 희소 데이터 문제 근본 해결 불가, 쿼리 여전히 비효율

2. **데이터 중복 (비정규화)**
   - GenerationHistory 정보를 ModelSample에 복사
   - 장점: JOIN 불필요
   - 단점: 데이터 일관성 문제, 관리 복잡

### 최종 선택
- **별도 테이블 분리 + FK 참조 + JOIN FETCH**
- 이유:
  - 희소 데이터 문제 완전 해결
  - 쿼리 성능 최적화
  - 데이터 정규화 유지 (일관성 보장)
  - JOIN FETCH로 N+1 문제 해결

**영향 받은 파일**:
- `ModelSample.java` (신규 생성)
- `ModelSampleRepository.java` (신규 생성)
- `SampleService.java` (신규 생성)
- `GeneratedImage.java` (isSample 필드 제거)
- `LoraModelService.java` (샘플 조회 로직 변경)
- `GenerationService.java` (샘플 확인 로직 변경)

---

## 4. TrainingJob ID 기반 학습 작업 관리 통합

### 문제 상황
초기에는 `userId` 기반으로 학습 작업을 관리했으나, 다음과 같은 문제가 발생:
- 같은 사용자가 여러 학습 작업을 동시에 시작하면 충돌 발생
- FastAPI와 Backend가 다른 식별자 사용 (userId vs modelId)
- S3 경로가 userId 기반이라 작업 간 충돌 가능
- 콜백에서 작업 식별이 불명확

### 해결 방법
**TrainingJob 테이블의 ID를 고유 식별자로 사용**

#### Backend 흐름
```java
// 1. TrainingJob 생성 (TrainingService.java)
TrainingJobResponse job = trainingService.createAndStartTrainingJob(
    userId, modelName, modelDescription, trainingImageUrls.size(),
    epochs, learningRate, loraRank, baseModel, triggerWord
);
// job.getId() → 고유한 TrainingJob ID 생성됨

// 2. FastAPI에 job_id 전달 (TrainingController.java)
fastApiClient.startTraining(
    userId.toString(),
    null,  // modelId는 학습 완료 후 생성
    job.getId(),  // ← TrainingJob ID
    modelName,
    trainingImageUrls,
    ...
);
```

#### FastAPI 변경사항 (main.py)
```python
class TrainRequest(BaseModel):
    user_id: str
    model_id: Optional[int]  # 학습 완료 후 생성될 LoraModel ID
    job_id: int  # ← TrainingJob 테이블의 ID (고유 식별자)
    model_name: str
    training_image_urls: List[str]
    ...

# job_id 기반 폴더 구조
dataset_path = f"temp/training-{job_id}/dataset"
output_dir = f"temp/training-{job_id}/model"

# S3 키 생성
s3_key = f"training-{job_id}/{model_name}.safetensors"

# SUCCESS 콜백
callback_data = {
    "userId": user_id,
    "modelId": model_id,
    "jobId": job_id,  # ← Backend가 이걸로 TrainingJob 찾음
    "modelName": model_name,
    "s3ModelKey": s3_key,
    "fileSize": file_size,
    "status": "SUCCESS"
}
```

#### Backend 콜백 처리 (TrainingController.java)
```java
// jobId로 TrainingJob 찾기 (우선순위 1)
if (jobId != null) {
    Long modelId = trainingService.handleTrainingSuccess(jobId, s3ModelKey, fileSize);
}
// jobId가 없으면 userId로 진행 중인 작업 찾기 (fallback)
else if (userId != null) {
    TrainingJobResponse activeJob = trainingService.getUserActiveTrainingJob(userId);
    jobId = activeJob.getId();
}
```

### 개선 효과
1. **고유 식별**: 각 학습 작업마다 고유한 ID
2. **충돌 방지**: 같은 유저가 여러 학습을 동시에 시작해도 문제 없음
3. **정확한 추적**: FastAPI와 Backend가 같은 ID로 동일한 작업 참조
4. **깔끔한 정리**: 학습 완료/실패 후 `training-{job_id}` 폴더 전체 삭제
5. **S3 관리**: 작업별로 독립된 S3 경로 (`training-{job_id}/`)

**영향 받은 파일**:
- Backend: `FastApiClient.java`, `TrainingController.java`, `TrainingService.java`
- FastAPI: `main.py` (TrainRequest, download_training_images, run_training_task)

---

## 5. WebSocket 백엔드 과부하 및 무한 재연결 문제 해결

### 문제 상황
1. **백엔드 Idle Timeout이 너무 짧음**
   - WebSocket idle timeout: 10분 (600,000ms)
   - 학습/생성 작업이 10분 넘게 걸리면 WebSocket 강제 종료
   - 프론트엔드가 5초마다 재연결 시도 → **백엔드 과부하** 발생

2. **프론트엔드 무한 재연결**
   - 재연결 시도 횟수 제한 없음
   - 학습 완료 후에도 SUCCESS 메시지를 못 받으면 계속 재연결
   - 네트워크 문제 시 영원히 재연결 시도

### 해결 방법

#### 1) Backend Timeout 증가 (WebSocketConfig.java)
```java
@Bean
public ServletServerContainerFactoryBean createWebSocketContainer() {
    ServletServerContainerFactoryBean container = new ServletServerContainerFactoryBean();
    container.setMaxSessionIdleTimeout(3600000L); // 10분 → 1시간
    container.setMaxTextMessageBufferSize(8192);
    container.setMaxBinaryMessageBufferSize(8192);
    return container;
}
```

#### 2) 프론트엔드 재연결 제한 (TrainingForm.vue, GenerateModal.vue)
```javascript
let reconnectAttempts = 0;
const MAX_RECONNECT_ATTEMPTS = 20; // 최대 20번 재연결 시도 (100초)

websocket.onopen = () => {
  reconnectAttempts = 0; // 연결 성공 시 카운터 리셋
};

websocket.onclose = (event) => {
  if (isTraining.value && reconnectAttempts < MAX_RECONNECT_ATTEMPTS) {
    reconnectAttempts++;
    console.log(`재연결 시도... (attempt ${reconnectAttempts}/${MAX_RECONNECT_ATTEMPTS})`);
    reconnectTimer = setTimeout(connectWebSocket, 5000);
  } else if (reconnectAttempts >= MAX_RECONNECT_ATTEMPTS) {
    console.error('최대 재연결 시도 횟수 초과. 연결을 중단합니다.');
    error.value = '서버 연결이 불안정합니다. 학습 상태를 확인해주세요.';
    isTraining.value = false;
  }
};

// SUCCESS/FAIL 시 WebSocket 종료 및 카운터 리셋
const disconnectWebSocket = () => {
  if (reconnectTimer) clearTimeout(reconnectTimer);
  if (heartbeatTimer) clearInterval(heartbeatTimer);
  if (websocket) websocket.close();
  reconnectAttempts = 0;
};
```

#### 3) Heartbeat 메커니즘
```javascript
// 30초마다 ping-pong 메시지 교환
heartbeatTimer = setInterval(() => {
  if (websocket && websocket.readyState === WebSocket.OPEN) {
    websocket.send(JSON.stringify({ type: 'ping' }));
  }
}, 30000);
```

### 타임라인 예시
```
0초   - 이미지 생성 시작, WebSocket 연결
30초  - Heartbeat ping → pong (연결 유지)
60초  - Heartbeat ping → pong (연결 유지)
90초  - ✅ FastAPI에서 첫 메시지 도착 → 정상 처리
...   - 학습/생성 완료
끝    - SUCCESS 메시지 수신 → WebSocket 자동 종료
```

### 개선 효과
1. **백엔드 안정성**: 1시간 timeout으로 장시간 작업도 연결 유지
2. **과부하 방지**: 최대 20번(100초) 재연결 후 중단
3. **사용자 경험**: 명확한 에러 메시지로 상태 확인 가능
4. **리소스 관리**: SUCCESS/FAIL 시 즉시 연결 종료 + 카운터 리셋

**영향 받은 파일**:
- Backend: `WebSocketConfig.java`
- Frontend: `TrainingForm.vue`, `GenerateModal.vue`

---

## 6. WebSocket 다중 사용자 및 다중 탭 지원

### 문제 상황
초기 구현에서는 `userId`를 키로 WebSocket 세션을 관리:
```java
// 문제가 있던 초기 설계
Map<Long, WebSocketSession> sessions = new ConcurrentHashMap<>();
sessions.put(userId, session); // 같은 userId면 이전 세션 덮어씌움!
```

#### 문제 시나리오
```
User A (id=1) 탭1 → 학습 시작 → sessions.put(1, session1)
User A (id=1) 탭2 → 이미지 생성 → sessions.put(1, session2)
                                     ↑ session1 덮어씌움!
```
**결과**: 탭1의 WebSocket이 끊김, 탭1은 학습 메시지를 받지 못함

### 해결 방법
**sessionId를 키로 사용하고, userId는 별도 매핑**

#### GenerationProgressHandler.java 수정
```java
// sessionId -> WebSocketSession 매핑 (동일 유저의 여러 탭/작업 지원)
private final Map<String, WebSocketSession> sessions = new ConcurrentHashMap<>();
// sessionId -> userId 매핑
private final Map<String, Long> sessionUserMap = new ConcurrentHashMap<>();

@Override
public void afterConnectionEstablished(WebSocketSession session) {
    String sessionId = session.getId();
    Long userId = extractUserIdFromQuery(query);

    sessions.put(sessionId, session);
    sessionUserMap.put(sessionId, userId);
}

public void sendToUser(Long userId, Object message) {
    // 해당 userId의 모든 세션에 메시지 전송
    int sentCount = 0;
    for (Map.Entry<String, Long> entry : sessionUserMap.entrySet()) {
        if (entry.getValue().equals(userId)) {
            String sessionId = entry.getKey();
            WebSocketSession session = sessions.get(sessionId);

            if (session != null && session.isOpen()) {
                session.sendMessage(new TextMessage(json));
                sentCount++;
            }
        }
    }
    log.info("📤 WebSocket 메시지 전송 완료: userId={}, sessions={}", userId, sentCount);
}
```

#### 프론트엔드 필터링
```javascript
// TrainingForm.vue
if (trainingJobId.value && data.jobId && data.jobId !== trainingJobId.value) {
  return; // 다른 작업의 메시지 무시
}

// GenerateModal.vue
if (data.historyId !== currentHistoryId) {
  return; // 다른 작업의 메시지 무시
}
```

### 작동 방식

#### 시나리오 1: 다른 유저들이 동시 사용 ✅
```
User A (id=1) → session-abc → sessions.put("abc", sessionA)
User B (id=2) → session-def → sessions.put("def", sessionB)
User C (id=3) → session-ghi → sessions.put("ghi", sessionC)
```
**결과**: 각자의 WebSocket으로 메시지 전달

#### 시나리오 2: 같은 유저가 여러 탭에서 동시 작업 ✅
```
User A (id=1) 탭1 → 학습 시작 → session-abc
                   sessions.put("abc", sessionA)
                   sessionUserMap.put("abc", 1)

User A (id=1) 탭2 → 이미지 생성 → session-xyz
                   sessions.put("xyz", sessionB)
                   sessionUserMap.put("xyz", 1)

// 메시지 전송 시
sendToUser(1, message) {
  // sessionUserMap에서 userId=1인 세션 모두 찾기
  // → "abc", "xyz" 발견
  // → 두 세션 모두에게 메시지 전송!
}
```

### 개선 효과
1. **동시 사용자 지원**: 여러 명이 동시에 학습/생성 가능
2. **다중 탭 지원**: 같은 유저가 여러 탭에서 동시 작업 가능
3. **정확한 메시지 전달**: 각 탭이 자신의 작업 메시지만 처리
4. **확장성**: 유저당 세션 수 제한 없음

**영향 받은 파일**:
- Backend: `GenerationProgressHandler.java`
- Frontend: `TrainingForm.vue`, `GenerateModal.vue` (jobId/historyId 필터링)

---

## 11. GPU 리소스 관리 및 대기열 시스템

### 문제 상황
- Modal GPU 컨테이너는 학습 + 생성을 합쳐서 최대 10개까지만 동시 실행 가능
- GPU 리소스가 부족할 때 요청을 즉시 거절하면 사용자 경험이 나쁨
- Modal 서버가 꺼져있을 때 "학습 중..." 무한 대기 문제

### 해결 방법

#### 1. Modal 서버 연결 확인 (동기 호출)
**비동기 호출 문제**:
```java
// 비동기: 요청만 보내고 기다리지 않음
fastApiClient.startTraining(...).subscribe(
    message -> { /* 성공 */ },
    error -> { /* 실패 */ }
);
// 즉시 성공 응답 반환 ❌
```

**동기 호출로 해결**:
```java
// 동기: Modal 응답을 10초 동안 대기
String response = fastApiClient.startTraining(...)
    .block(Duration.ofSeconds(10));
// Modal이 응답해야만 성공 반환 ✅
```

- **FastApiClient.java**: HTTP 클라이언트에 10초 타임아웃 설정
- **TrainingController.java / GenerationController.java**: `.block()` 사용
- Modal 서버가 꺼져있으면 10초 후 명확한 에러 메시지 반환

#### 2. GPU 대기열 시스템 (GpuResourceManager)
**구조**:
```
[사용자 요청] → [큐에 추가] → [GPU 여유 생김] → [자동 실행]
```

**주요 기능**:
- **Semaphore**: GPU 슬롯 10개 관리
- **BlockingQueue**: 대기 중인 작업 저장
- **백그라운드 스레드**: GPU 여유 생기면 자동으로 큐에서 꺼내서 실행

**사용 방법**:
```java
// 작업을 큐에 추가 (즉시 반환)
gpuResourceManager.enqueueTask(
    jobId,
    TaskType.TRAINING,
    () -> {
        // GPU 여유 생기면 실행될 작업
        fastApiClient.startTraining(...).block();
    }
);

// 작업 완료 시 리소스 반환
gpuResourceManager.release(jobId);
```

**동작 과정**:
1. 사용자가 학습/생성 요청
2. 큐에 추가 후 즉시 "요청이 접수되었습니다" 응답
3. GPU 여유 생기면 자동으로 Modal 호출
4. 학습/생성 완료 시 GPU 리소스 반환
5. 다음 대기 중인 작업 자동 실행

**장점**:
- ✅ GPU가 꽉 차도 대기하면 언젠가 실행됨
- ✅ 사용자는 즉시 응답 받음 (대기열에 추가됨)
- ✅ 학습 완료 후 자동으로 다음 작업 실행

**상태 조회 API**:
```
GET / (헬스 체크)
{
  "gpu": {
    "total": 10,
    "used": 7,
    "available": 3,
    "queued": 5  // 대기 중인 작업 수
  }
}
```

### 영향 받은 파일
**Backend**:
- `GpuResourceManager.java` (신규) - GPU 리소스 및 대기열 관리
- `FastApiClient.java` - 10초 타임아웃 설정
- `TrainingController.java` - 동기 호출 + 큐 사용
- `GenerationController.java` - 동기 호출 + 큐 사용
- `HealthCheckController.java` - GPU 상태 포함

**Modal**:
- `modal_app.py`:
  - `LoraTrainer`: `concurrency_limit=10` 추가
  - `ImageGenerator`: `concurrency_limit=10` 추가
  - `@modal.exit()` 훅 - 강제 종료 시 실패 콜백 전송

### 주의사항
- 서버 재시작 시 큐가 초기화됨 (메모리 기반)
- 프로덕션에서는 Redis 기반 큐 권장
- Modal의 `concurrency_limit`는 클래스별 제한 (학습 10개 + 생성 10개 = 총 20개 가능)
- 백엔드 `GpuResourceManager`가 전체 10개 제한 (학습 + 생성 합쳐서)
